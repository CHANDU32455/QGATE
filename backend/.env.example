# QGATE Backend Production Environment Template
# Use this to configure your AWS App Runner or Render environment variables

# --- Server Config ---
NODE_ENV=production
PORT=5000
# ALLOWED_ORIGINS=https://your-web-client.com,https://your-other-domain.com

# --- Database ---
# Get this from MongoDB Atlas (e.g. mongodb+srv://<username>:<password>@cluster.mongodb.net/dbname)
MONGO_URI=

# --- Cache ---
# Get this from AWS ElastiCache (Redis) or Railway/Render
# Format: redis://[:password]@host:port
REDIS_URL=redis://localhost:6379

# --- Security (Optional) ---
# Play Integrity API Key for Android Hardware Attestation
PLAY_INTEGRITY_API_KEY=
ALLOWED_ANDROID_PACKAGES=com.qgate.auth

# --- QRNG (Optional) ---
# Custom provider for true quantum randomness
# QRNG_PROVIDER_URL=https://qrng.anu.edu.au/API/jsonI.php
